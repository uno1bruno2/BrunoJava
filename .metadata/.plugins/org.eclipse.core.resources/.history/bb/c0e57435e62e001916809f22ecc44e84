package main;

import java.util.stream.Collectors;

public class IsPrime {
	
	public List<Integer> primes(List<Integer> numbers) {
		
		return numbers.stream().filter(x -> x > 2).collect(Collectors.toList());
		
	}
	
	
	
	
}
